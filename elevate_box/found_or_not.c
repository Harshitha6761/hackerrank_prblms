#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n;
    scanf("%d",&n);
    if(n==404)
        printf("not found");
    else
        printf("found");

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    return 0;
}
